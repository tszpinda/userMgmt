<nav {{bind-attr class=":nav :navbar-default :navbar-static-top :session user:logged-in:logged-out"}}>
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#nav-menu">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">TutThem</a>
        </div>
        <div class="collapse navbar-collapse navbar-right" id="nav-menu">
            <ul class="nav navbar-nav">
            {{#if user}}
                {{#link-to 'tutorials' tagName="li" href=false}}
                    <button href="#" class="btn btn-default navbar-btn">Tutorials</button>
                {{/link-to}}
                <li><button {{action "logout"}} class="btn btn-default navbar-btn">Logout({{user.name}})</a></li>
            {{else}}
                {{#link-to 'signup' tagName="li" href=false}}
                    <button href="#" class="btn btn-default navbar-btn">Sign Up</button>
                {{/link-to}}
                {{#link-to 'login' tagName="li" href=false}}
                    <button href="#" class="btn btn-default navbar-btn">Login</button>
                {{/link-to}}
            {{/if}}
            </ul>
        </div>
    </div>
</nav>

